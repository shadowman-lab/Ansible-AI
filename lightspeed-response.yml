---
- name: Restart HTTPD
  hosts: dynatrace.shadowman.dev
  tasks:
    - name: Copy file
      ansible.builtin.copy:
        src: files/httpd.conf
        dest: /etc/httpd/conf/httpd.conf
    - name: Restart service
      ansible.builtin.service:
        name: httpd
        state: restarted
